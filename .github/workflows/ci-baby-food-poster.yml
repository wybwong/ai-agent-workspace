name: CI — baby-food-poster

on:
  push:
    branches: [main]
    paths:
      - 'projects/baby-food-poster/**'
  pull_request:
    branches: [main]
    paths:
      - 'projects/baby-food-poster/**'
  workflow_dispatch:

env:
  BASE_PATH: /ai-agent-workspace/projects/baby-food-poster/

jobs:
  build:
    name: Build baby-food-poster
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: projects/baby-food-poster

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: projects/baby-food-poster/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type check & Build
        run: npm run build -- --base=${{ env.BASE_PATH }}
