name: Deploy to Self-Hosted Server

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: projects/baby-food-poster
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: projects/baby-food-poster/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      # burnett01/rsync-deployments action runs from repo root, not affected by defaults.run.working-directory
      - name: Deploy via rsync over SSH
        uses: burnett01/rsync-deployments@7.0.1
        with:
          switches: -avzr --delete
          path: projects/baby-food-poster/dist/
          remote_path: ${{ secrets.DEPLOY_PATH }}
          remote_host: ${{ secrets.SERVER_HOST }}
          remote_port: ${{ secrets.SERVER_PORT }}
          remote_user: ${{ secrets.SERVER_USER }}
          remote_key: ${{ secrets.SERVER_SSH_KEY }}

      - name: Notify deploy success
        run: echo "✅ Deployed to ${{ secrets.SERVER_HOST }}:${{ secrets.DEPLOY_PATH }}"
